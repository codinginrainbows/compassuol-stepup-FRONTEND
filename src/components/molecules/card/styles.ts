import styled from 'styled-components';

export const Wrapper = styled.div`
    background-color: #1E1F23;
    border: 2px solid #2E2F36;
    border-radius: var(--default);
    padding: var(--default);
    margin-top: calc(var(--default) * 2);
`