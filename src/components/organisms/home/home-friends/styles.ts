import styled from 'styled-components';

export const Wrapper = styled.div`
    margin-right: calc(var(--default) * 2);
`

export const List = styled.div`
    width: 100%;
    display: flex;
    align-items: center;
    margin-top: 0.5rem;
`